% ========================================
% MULTIPLE COLUMNS
% Set page size and margins
% ========================================


% MULTICOL
% The package multicol enables to use multiple columns flexibly in the document:
\usepackage{multicol}

% Multiple columns environment:
% - Use the multicols environment for a balanced multiple columns layout
% - Use the multicols* environment for an unbalanced multiple columns layout
% - Attribute: number of columns
% - Use the \columnbreak command to insert a column break
% \begin{multicols}{3} \end{multicols}
% \begin{multicols*}{3} \end{multicols*}

% Columns separator size and color:
% - Uncomment these to use column separators with specific size and color
% \setlength{\columnseprule}{1pt}
% \def\columnseprulecolor{\color{blue}}

% FIGURES IN MULTICOL ENVIRONMENT
% Use either wrapfigure (figure adjust to column) or figure* (figure spans over the entire page)
